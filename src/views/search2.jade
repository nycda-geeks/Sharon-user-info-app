extends layout

block content

	h2 Search users

	div#formdiv
		form(id="personlist", name="searchUser", action="/result", method="post", autocomplete="off")
			div.input
				span.label Search name  
				input#searchlist(type="text", name="userinput")
			div#people
			div.actions
				input#button(type="submit", value="search")

